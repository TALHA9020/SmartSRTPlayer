<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- سبٹائٹل ایریا (پوائنٹ 5، 6، 8، 9) -->
    <LinearLayout
        android:id="@+id/subtitle_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="200dp"
        android:padding="10dp"
        android:background="#80000000"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/timer_display"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00:00"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/subtitle_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="اردو سبٹائٹل"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textAlignment="center"
            android:gravity="center" />
    </LinearLayout>

    <!-- کنٹرولز ایریا (پوائنٹ 1، 2، 3، 7) -->
    <LinearLayout
        android:id="@+id/controls_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/subtitle_container"
        android:layout_centerHorizontal="true"
        android:background="#CC111111"
        android:orientation="horizontal"
        android:padding="5dp"
        android:layout_marginTop="5dp">

        <ImageButton android:id="@+id/btn_offset_minus" android:layout_width="35dp" android:layout_height="35dp" android:src="@android:drawable/ic_menu_minus" />
        <ImageButton android:id="@+id/btn_backward" android:layout_width="40dp" android:layout_height="40dp" android:src="@android:drawable/ic_media_rew" />
        <ImageButton android:id="@+id/btn_play_pause" android:layout_width="50dp" android:layout_height="50dp" android:src="@android:drawable/ic_media_play" />
        <ImageButton android:id="@+id/btn_forward" android:layout_width="40dp" android:layout_height="40dp" android:src="@android:drawable/ic_media_ff" />
        <ImageButton android:id="@+id/btn_offset_plus" android:layout_width="35dp" android:layout_height="35dp" android:src="@android:drawable/ic_menu_add" />
        <ImageButton android:id="@+id/btn_hide_bg" android:layout_width="35dp" android:layout_height="35dp" android:src="@android:drawable/ic_menu_view" />
    </LinearLayout>

</RelativeLayout>
